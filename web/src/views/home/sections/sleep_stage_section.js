import React from 'react';
import { Container } from 'reactstrap';

const SleepStageSection = () => (
  <section className="section mt-4">
    <Container>
      <h2 className="mt-6">Why sleep stages?</h2>
      <p>This paragraph will explain why it is useful to classify sleep into sleep stages.</p>
    </Container>
  </section>
);

export default SleepStageSection;
