import 'package:flutter/material.dart';

// todo: build analyzing section
Widget buildAnalyzeInProgress(BuildContext context) {
  return Center(
    child: Column(
        mainAxisAlignment: MainAxisAlignment.center,
        children: <Widget>[Text('Analyzing')]),
  );
}
